server.port=8761
# 本身是服务端，所以不需要注册到eureka-server(默认为true)
eureka.client.register-with-eureka=false
# 是否从eureka-server 获取注册信息，这个项目是服务端，所以也不需要（默认是ture）
eureka.client.fetch-remote-regions-registry=false
# 是否从其他eureka-server获取注册服务信息 （该项目时单点eureka-server，所以不需要）
eureka.client.fetch-registry=false
# 设置eureka-server的地址（多个地址使用逗号分隔）
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
